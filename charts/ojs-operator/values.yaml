# OJS Operator Helm Values

# Operator image configuration
image:
  repository: ghcr.io/openjobspec/ojs-k8s-operator
  tag: ""  # Defaults to appVersion
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# Operator deployment settings
replicaCount: 1

# Leader election (recommended for HA)
leaderElection:
  enabled: true

# Resource requirements for the operator pod
resources:
  limits:
    cpu: 500m
    memory: 128Mi
  requests:
    cpu: 10m
    memory: 64Mi

# Security context for the operator pod
podSecurityContext:
  runAsNonRoot: true

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  capabilities:
    drop:
      - ALL

# Service account
serviceAccount:
  create: true
  annotations: {}
  name: ""

# RBAC
rbac:
  create: true

# CRD installation
crds:
  install: true

# Health probes
healthProbe:
  port: 8081

# Pod Disruption Budget
podDisruptionBudget:
  enabled: false
  minAvailable: 1
  # maxUnavailable: 1

# ServiceMonitor RBAC (optional, for prometheus-operator integration)
serviceMonitor:
  rbac:
    enabled: true

# Webhooks
# ⚠️  NOTE: Webhooks are DISABLED by default. When disabled, OJSCluster and OJSWorker
# custom resources are NOT validated on admission — invalid specs may reach the cluster.
#
# To enable webhook validation, you need one of:
#   1. cert-manager installed → set webhook.enabled=true and webhook.certManager.enabled=true
#   2. Manual TLS certs → set webhook.enabled=true and provide certs via a Secret
#
# Recommended for production: enable webhooks with cert-manager to catch invalid CRDs early.
webhook:
  enabled: false
  failurePolicy: Fail
  certManager:
    enabled: false
    issuerRef:
      kind: Issuer
      name: ""  # Auto-populated with self-signed issuer if empty

# Node selector, tolerations, affinity
nodeSelector: {}
tolerations: []
affinity: {}

# Additional labels for all resources
commonLabels: {}

# Additional annotations for the operator deployment
podAnnotations: {}

